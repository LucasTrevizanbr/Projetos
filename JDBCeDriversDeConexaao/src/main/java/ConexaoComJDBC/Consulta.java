package ConexaoComJDBC;

public class Consulta {
    /*
        Existem 3 interfaces para executar comandos SQL. Statement: executar SQL comuns,
        PreparedStatement: executar SQL parametrizáveis e CallableStatement: Executar stored Procedures.
        É preferível usar a PreparedStatement ao Statement.
              Existem 3 métodos para executar consulta comandos SQL. execute: pode executar qualquer
        tipo de SQL, executeQuery: usado para executar "SELECT" e executeUpdate: usado para comandos
        de alteração de banco de dados(INSERT, UPDATE, DELETE, CREATE, ALTER).
               Quando buscamos os dados do banco retornamos um ResultSet: um objeto que contém os
        dados de uma determinada consulta(normalmente SELECT). São usados getters para buscar os
        dados do ResultSet. O método next() é utilizado para percorrer os registros do ResultSet,




     */
}
